# Copy to terraform.tfvars (gitignored).

cloudflare_api_token = "your-token"
cloudflare_zone_id   = "your-zone-id"

# Optional; all record names are already relative to this zone.
# domain = "joshi1.com"
